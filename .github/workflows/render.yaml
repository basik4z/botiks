services:
  - type: web
    name: botiks
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
    plan: free
    region: oregon
    pythonVersion: "3.11"